diff --git a/src/app.html b/src/app.html
index 8ab0372..de169f0 100644
--- a/src/app.html
+++ b/src/app.html
@@ -2,11 +2,11 @@
 <html lang="en">
 	<head>
 		<meta charset="utf-8" />
-		<link rel="icon" href="%sveltekit.assets%/favicon.png" />
-		<link rel="stylesheet" href="/fonts/inter/inter.css" />
-		<link rel="stylesheet" href="/fonts/jetbrains-mono/jetbrains-mono.css" />
+		<link rel="icon" href="%sveltekit.assets%/favicon.svg" type="image/svg+xml" />
+		<link rel="stylesheet" href="%sveltekit.assets%/fonts/hologram/hologram.css" />
 		<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1" />
-		<title>Hollama</title>
+		<title>Hologram</title>
+		<script type="module" src="%sveltekit.assets%/ui-host.js"></script>
 		%sveltekit.head%
 	</head>
 	<script>
@@ -15,7 +15,7 @@
 		let theme = JSON.parse(localStorage.getItem('hollama-settings'))?.userTheme;
 		if (theme) document.documentElement.setAttribute('data-color-theme', theme);
 		else {
-			theme = window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
+			theme = 'dark';
 			document.documentElement.setAttribute('data-color-theme', theme);
 		}
 	</script>
diff --git a/src/lib/components/ButtonDelete.svelte b/src/lib/components/ButtonDelete.svelte
index 7c7e25b..c3a374b 100644
--- a/src/lib/components/ButtonDelete.svelte
+++ b/src/lib/components/ButtonDelete.svelte
@@ -1,4 +1,5 @@
 <script lang="ts">
+	import { base } from '$app/paths';
 	import { Check, Trash2, X } from 'lucide-svelte';
 
 	import LL from '$i18n/i18n-svelte';
@@ -22,11 +23,11 @@
 		switch (sitemap) {
 			case Sitemap.KNOWLEDGE:
 				if ($knowledgeStore) $knowledgeStore = deleteStoreItem($knowledgeStore, id);
-				return goto('/knowledge');
+				return goto(`${base}/knowledge`);
 
 			case Sitemap.SESSIONS:
 				if ($sessionsStore) $sessionsStore = deleteStoreItem($sessionsStore, id);
-				return goto('/sessions');
+				return goto(`${base}/sessions`);
 
 			default:
 				break;
diff --git a/src/lib/components/ButtonNew.ts b/src/lib/components/ButtonNew.ts
index bdbcdd3..2164880 100644
--- a/src/lib/components/ButtonNew.ts
+++ b/src/lib/components/ButtonNew.ts
@@ -1,6 +1,7 @@
+import { base } from '$app/paths';
 import { type Sitemap } from '$lib/sitemap';
 import { generateRandomId } from '$lib/utils';
 
 export function generateNewUrl(sitemap: Sitemap, id?: string): string {
-	return `/${sitemap}/${id ? id : generateRandomId()}`;
+	return `${base}/${sitemap}/${id ? id : generateRandomId()}`;
 }
diff --git a/src/lib/components/CollapsibleSidebar.svelte b/src/lib/components/CollapsibleSidebar.svelte
index da4375f..5c9235e 100644
--- a/src/lib/components/CollapsibleSidebar.svelte
+++ b/src/lib/components/CollapsibleSidebar.svelte
@@ -1,10 +1,10 @@
 <script lang="ts">
+	import { assets, base } from '$app/paths';
 	import {
 		Brain,
 		Github,
 		MessageSquareText,
 		Moon,
-		NotebookText,
 		Settings2,
 		Sun
 	} from 'lucide-svelte';
@@ -45,9 +45,9 @@
 	function setActiveSection(section: SidebarSection) {
 		activeSection = section;
 		if (section === 'sessions') {
-			goto('/sessions');
+			goto(`${base}/sessions`);
 		} else if (section === 'knowledge') {
-			goto('/knowledge');
+			goto(`${base}/knowledge`);
 		}
 	}
 </script>
@@ -66,9 +66,9 @@
 			data-testid="sidebar"
 		>
 			<div class="flex items-center justify-between border-b py-4">
-				<a href="/" class="mx-auto flex items-center gap-2 pr-4">
-					<img class="h-8 w-8" src="/favicon.png" alt="Hollama logo" />
-					<span class="text-lg font-semibold tracking-tight">Hollama</span>
+				<a href="{base}/" class="mx-auto flex items-center gap-2 pr-4">
+					<img class="h-8 w-8" src="{assets}/favicon.svg" alt="Hologram" />
+					<span class="text-lg font-semibold tracking-tight">Hologram</span>
 				</a>
 			</div>
 
@@ -159,20 +159,7 @@
 
 			<div class="border-t px-2 py-3">
 				<a
-					href="/motd"
-					class="duration-25 flex items-center gap-3 px-4 py-3 text-sm font-medium transition-colors hover:text-active {pathname.includes(
-						'/motd'
-					)
-						? 'text-active'
-						: 'text-muted'}"
-					aria-current={pathname.includes('/motd') ? 'page' : undefined}
-				>
-					<NotebookText class="h-4 w-4" />
-					{$LL.motd()}
-				</a>
-
-				<a
-					href="/settings"
+					href="{base}/settings"
 					class="duration-25 relative flex items-center gap-3 px-4 py-3 text-sm font-medium transition-colors hover:text-active {pathname.includes(
 						'/settings'
 					)
@@ -187,7 +174,7 @@
 				</a>
 
 				<a
-					href="https://github.com/fmaclen/hollama"
+					href="https://github.com/Hologram-Technologies"
 					target="_blank"
 					rel="noopener noreferrer"
 					class="duration-25 flex items-center gap-3 px-4 py-3 text-sm font-medium text-muted transition-colors hover:text-active"
diff --git a/src/lib/components/Head.svelte b/src/lib/components/Head.svelte
index 8defcad..1e727a6 100644
--- a/src/lib/components/Head.svelte
+++ b/src/lib/components/Head.svelte
@@ -4,8 +4,8 @@
 	export let title: LocalizedString | LocalizedString[] | string | string[] | undefined = undefined;
 	const SEPARATOR = ' • ';
 	$: formattedTitle = title
-		? (Array.isArray(title) ? title.join(SEPARATOR) : title) + SEPARATOR + 'Hollama'
-		: 'Hollama';
+		? (Array.isArray(title) ? title.join(SEPARATOR) : title) + SEPARATOR + 'Hologram'
+		: 'Hologram';
 </script>
 
 <svelte:head>
diff --git a/src/lib/components/SectionListItem.svelte b/src/lib/components/SectionListItem.svelte
index effb64d..0f8aebc 100644
--- a/src/lib/components/SectionListItem.svelte
+++ b/src/lib/components/SectionListItem.svelte
@@ -1,4 +1,5 @@
 <script lang="ts">
+	import { base } from '$app/paths';
 	import LL from '$i18n/i18n-svelte';
 	import { page } from '$app/state';
 	import { sessionsStore } from '$lib/localStorage';
@@ -75,7 +76,7 @@
 				class="section-list-item__a"
 				data-testid={isSession ? 'session-item' : 'knowledge-item'}
 				aria-label={(isSession ? $LL.session() : $LL.knowledge()) + `: ${id}`}
-				href={`/${sitemap}/${id}`}
+				href={`${base}/${sitemap}/${id}`}
 			>
 				<p class="section-list-item__title">
 					{title}
diff --git a/src/lib/github.ts b/src/lib/github.ts
index 3912652..14ac2a2 100644
--- a/src/lib/github.ts
+++ b/src/lib/github.ts
@@ -1,2 +1,2 @@
-export const GITHUB_RELEASES_API = 'https://api.github.com/repos/fmaclen/hollama/releases';
-export const GITHUB_RELEASES_URL = 'https://github.com/fmaclen/hollama/releases';
+export const GITHUB_RELEASES_API = 'https://api.github.com/repos/humuhumu33/dyad-prism/releases';
+export const GITHUB_RELEASES_URL = 'https://github.com/humuhumu33/dyad-prism/releases';
diff --git a/src/lib/settings.ts b/src/lib/settings.ts
index 9e9a367..1a5461b 100644
--- a/src/lib/settings.ts
+++ b/src/lib/settings.ts
@@ -28,7 +28,7 @@ export const DEFAULT_SETTINGS: Settings = {
 	lastUsedModels: [],
 	lastUpdateCheck: null,
 	autoCheckForUpdates: false,
-	userTheme: 'light',
+	userTheme: 'dark',
 	userLanguage: null,
 	sidebarExpanded: true,
 	hollamaMetadata: {
diff --git a/src/lib/updates.ts b/src/lib/updates.ts
index 30e7414..86a3663 100644
--- a/src/lib/updates.ts
+++ b/src/lib/updates.ts
@@ -3,13 +3,14 @@ import semver from 'semver';
 import { get, writable } from 'svelte/store';
 
 import { version } from '$app/environment';
+import { base } from '$app/paths';
 import { settingsStore } from '$lib/localStorage';
 
 import type { HollamaMetadata } from '../routes/api/metadata/+server';
 import { GITHUB_RELEASES_API } from './github';
 
 const HOLLAMA_DEV_VERSION_SUFFIX = '-dev';
-const HOLLAMA_METADATA_ENDPOINT = '/api/metadata';
+const HOLLAMA_METADATA_ENDPOINT = `${base}/api/metadata`;
 const ONE_WEEK_IN_SECONDS = 604800;
 
 export interface UpdateStatus {
diff --git a/src/routes/+layout.svelte b/src/routes/+layout.svelte
index 76771b4..9ef6147 100644
--- a/src/routes/+layout.svelte
+++ b/src/routes/+layout.svelte
@@ -105,9 +105,7 @@
 
 		// Color theme
 		if (browser && !$settingsStore.userTheme) {
-			$settingsStore.userTheme = window.matchMedia('(prefers-color-scheme: dark)').matches
-				? 'dark'
-				: 'light';
+			$settingsStore.userTheme = 'dark';
 		}
 	});
 </script>
diff --git a/src/routes/+page.svelte b/src/routes/+page.svelte
index a3b1a6e..99823f0 100644
--- a/src/routes/+page.svelte
+++ b/src/routes/+page.svelte
@@ -1,14 +1,17 @@
 <script lang="ts">
+	import { base } from '$app/paths';
 	import { goto } from '$app/navigation';
+	import { generateNewUrl } from '$lib/components/ButtonNew';
 	import { serversStore } from '$lib/localStorage';
+	import { Sitemap } from '$lib/sitemap';
 
 	$effect.pre(() => {
 		// If at least one server is verified, redirect to the sessions page
 		if ($serversStore.some((server) => server.isVerified !== null)) {
-			goto('/sessions');
+			goto(generateNewUrl(Sitemap.SESSIONS));
 		} else {
 			// Otherwise, redirect to the settings page to add or verify servers
-			goto('/settings');
+			goto(`${base}/settings`);
 		}
 	});
 </script>
diff --git a/src/routes/api/metadata/+server.ts b/src/routes/api/metadata/+server.ts
index db601ac..f41f995 100644
--- a/src/routes/api/metadata/+server.ts
+++ b/src/routes/api/metadata/+server.ts
@@ -1,6 +1,7 @@
 import { json } from '@sveltejs/kit';
 
-import { env } from '$env/dynamic/public';
+export const prerender = true;
+
 import { version } from '$app/environment';
 
 export interface HollamaMetadata {
@@ -13,7 +14,7 @@ export interface HollamaMetadata {
 export async function GET() {
 	return json({
 		currentVersion: version,
-		isDesktop: env.PUBLIC_ADAPTER === 'electron-node',
-		isDocker: env.PUBLIC_ADAPTER === 'docker-node'
+		isDesktop: false,
+		isDocker: false
 	} as HollamaMetadata);
 }
diff --git a/src/routes/knowledge/[id]/+page.svelte b/src/routes/knowledge/[id]/+page.svelte
index 26d5c41..b8a0904 100644
--- a/src/routes/knowledge/[id]/+page.svelte
+++ b/src/routes/knowledge/[id]/+page.svelte
@@ -1,4 +1,5 @@
 <script lang="ts">
+	import { base } from '$app/paths';
 	import { toast } from 'svelte-sonner';
 
 	import LL from '$i18n/i18n-svelte';
@@ -51,7 +52,7 @@
 		{#snippet headline()}
 			<p data-testid="knowledge-id" class="font-bold leading-none">
 				{$LL.knowledge()}
-				<Button variant="link" href={`/knowledge/${knowledge.id}`}>
+				<Button variant="link" href={`${base}/knowledge/${knowledge.id}`}>
 					#{knowledge.id}
 				</Button>
 			</p>
diff --git a/src/routes/sessions/[id]/+page.svelte b/src/routes/sessions/[id]/+page.svelte
index edeaca8..d07ccd2 100644
--- a/src/routes/sessions/[id]/+page.svelte
+++ b/src/routes/sessions/[id]/+page.svelte
@@ -1,4 +1,5 @@
 <script lang="ts">
+	import { base } from '$app/paths';
 	import { onMount, tick } from 'svelte';
 	import { toast } from 'svelte-sonner';
 
@@ -60,6 +61,10 @@
 		session.model = $settingsStore.models.find((m) => m.name === modelName);
 	});
 
+	$effect(() => {
+		if (!modelName && $settingsStore.models.length) modelName = $settingsStore.models[0].name;
+	});
+
 	$effect(() => {
 		if (editor.shouldFocusTextarea && editor.promptTextarea) {
 			editor.promptTextarea.focus();
@@ -88,7 +93,7 @@
 		if (
 			editor.prompt &&
 			editor.prompt.trim() !== '' &&
-			!navigation.to?.url.pathname.startsWith('/sessions/')
+			!navigation.to?.url.pathname.startsWith(`${base}/sessions/`)
 		) {
 			const userConfirmed = confirm($LL.unsavedChangesWillBeLost());
 			if (!userConfirmed) {
@@ -305,7 +310,7 @@
 		{#snippet headline()}
 			<p data-testid="session-id" class="font-bold leading-none">
 				{$LL.session()}
-				<Button variant="link" href={`/sessions/${session.id}`}>#{session.id}</Button>
+				<Button variant="link" href={`${base}/sessions/${session.id}`}>#{session.id}</Button>
 			</p>
 			<Metadata dataTestid="session-metadata">
 				{editor.isNewSession ? $LL.newSession() : formatSessionMetadata(session)}
diff --git a/src/routes/settings/Version.svelte b/src/routes/settings/Version.svelte
index ca980dc..a9bfedb 100644
--- a/src/routes/settings/Version.svelte
+++ b/src/routes/settings/Version.svelte
@@ -63,7 +63,7 @@
 						{:else if $settingsStore.hollamaMetadata.isDocker}
 							<Button
 								variant="link"
-								href="https://github.com/fmaclen/hollama/blob/main/SELF_HOSTING.md#updating-to-the-latest-version"
+								href="https://github.com/humuhumu33/dyad-prism#readme"
 								target="_blank"
 							>
 								{$LL.howToUpdateDocker()}
diff --git a/svelte.config.js b/svelte.config.js
index d86925d..dc0ae25 100644
--- a/svelte.config.js
+++ b/svelte.config.js
@@ -1,18 +1,9 @@
-import adapterCloudflare from '@sveltejs/adapter-cloudflare';
-import adapterNode from '@sveltejs/adapter-node';
+import adapterStatic from '@sveltejs/adapter-static';
 import { vitePreprocess } from '@sveltejs/vite-plugin-svelte';
 import dotenv from 'dotenv';
 
 dotenv.config();
 
-const adapterConfig = {
-	// See below for an explanation of these options
-	routes: {
-		include: ['/*'],
-		exclude: ['<all>']
-	}
-};
-
 /** @type {import('@sveltejs/kit').Config} */
 const config = {
 	// Consult https://kit.svelte.dev/docs/integrations#preprocessors
@@ -23,9 +14,8 @@ const config = {
 		// adapter-auto only supports some environments, see https://kit.svelte.dev/docs/adapter-auto for a list.
 		// If your environment is not supported or you settled on a specific environment, switch out the adapter.
 		// See https://kit.svelte.dev/docs/adapters for more information about adapters.
-		adapter: ['docker-node', 'electron-node'].includes(process.env.PUBLIC_ADAPTER)
-			? adapterNode(adapterConfig)
-			: adapterCloudflare(adapterConfig),
+		adapter: adapterStatic({ fallback: 'index.html', strict: false }),
+		paths: { base: process.env.PUBLIC_BASE_PATH || '' },
 		version: {
 			name: process.env.npm_package_version
 		},
diff --git a/tailwind.config.js b/tailwind.config.js
index e376780..5e18f10 100644
--- a/tailwind.config.js
+++ b/tailwind.config.js
@@ -7,8 +7,8 @@ const config = {
 	theme: {
 		extend: {
 			fontFamily: {
-				sans: ['Inter', ...fontFamily.sans],
-				mono: ['JetBrains Mono', ...fontFamily.mono]
+				sans: ['Geist', ...fontFamily.sans],
+				mono: ['Geist Mono', ...fontFamily.mono]
 			},
 			backgroundColor: {
 				DEFAULT: 'hsl(var(--color-shade-2))'
